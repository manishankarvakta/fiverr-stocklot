version: "3.8"

services:
  mongodb:
    image: mongo:7.0
    container_name: stocklot-mongodb
    ports:
      - "27025:27017"  # Changed host port to 27025
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=password123
    restart: unless-stopped
    networks:
      - dokploy-network

  backend:
    build:
      context: ./backend
    container_name: stocklot-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - /app/venv  # Prevent venv from being mounted
    environment:
      - PYTHONUNBUFFERED=1
      - MONGODB_URL=mongodb://admin:password123@mongodb:27025  # Updated port
    depends_on:
      - mongodb
    restart: unless-stopped
    networks:
      - dokploy-network

  frontend:
    build:
      context: ./frontend
    container_name: stocklot-frontend
    ports:
      - "3030:80"
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - dokploy-network

volumes:
  mongodb_data:

networks:
  dokploy-network:
    external: true
